<StackPanel
    x:Class="RomanApp.Client.UWP.Views.Event.Sheet.Controls.Overview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomanApp.Client.UWP.Views.Event.Sheet.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Guests" FontSize="24"
                   FontWeight="Bold" />

        <local:Items Grid.Row="1" 
                     Symbol="Contact"
                     ItemsSource="{Binding Guests}" 
                     Margin="5" />
    </Grid>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Expense" FontSize="24" 
                   FontWeight="Bold" />

        <local:Items Grid.Row="1" 
                     Symbol="Pictures"
                     ItemsSource="{Binding Expenses}" 
                     Margin="5" />
    </Grid>

</StackPanel>
